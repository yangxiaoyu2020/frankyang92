[tool.poetry]
name = "algorithm"
version = "0.1.0"
description = ""
authors = ["frank yang <franksjty@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9"
psutil = "^5.9.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
